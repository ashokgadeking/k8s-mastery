apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "sa-helm.fullname" . }}-frontend
spec:
  replicas: {{ .Values.frontend.replicaCount }}
  minReadySeconds: {{ .Values.frontend.minReadySeconds }}
  strategy:
    type: {{ .Values.frontend.strategy.type }}
    rollingUpdate: 
      maxUnavailable: {{ .Values.frontend.strategy.rollingUpdate.maxUnavailable }}
      maxSurge: {{ .Values.frontend.strategy.rollingUpdate.maxSurge }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "sa-helm.name" . }}-frontend
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "sa-helm.name" . }}-frontend
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.frontend.image.repository }}:{{ .Values.frontend.image.tag }}"
          imagePullPolicy: {{ .Values.frontend.image.pullPolicy }}
          env:
            - name: SA_LOGIC_API_URL
              value: "http://sa-logic"
          ports:
            - containerPort: 8080
