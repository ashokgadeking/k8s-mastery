apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "sa-helm.fullname" . }}-webapp
spec:
  replicas: {{ .Values.sawebapp.replicaCount }}
  minReadySeconds: {{ .Values.sawebapp.minReadySeconds }}
  strategy:
    type: {{ .Values.sawebapp.strategy.type }}
    rollingUpdate: 
      maxUnavailable: {{ .Values.sawebapp.strategy.rollingUpdate.maxUnavailable }}
      maxSurge: {{ .Values.sawebapp.strategy.rollingUpdate.maxSurge }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "sa-helm.name" . }}-webapp
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "sa-helm.name" . }}-webapp
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.sawebapp.image.repository }}:{{ .Values.sawebapp.image.tag }}"
          imagePullPolicy: {{ .Values.sawebapp.image.pullPolicy }}
          env:
            - name: SA_LOGIC_API_URL
              value: "http://{{ include "sa-helm.fullname" . }}-logic"
          ports:
            - containerPort: 8080
